# Fast mode

Set `speed: "fast"` with the beta header on your request to opt in.

Fast mode is supported on the following models:

- Claude Opus 4.8 (`claude-opus-4-8`)
- Claude Opus 4.7 (`claude-opus-4-7`)

Fast mode is not available with the [Batch API](https://example.test).
