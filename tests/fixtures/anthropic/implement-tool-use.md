# How to implement tool use

<Note>
  Claude Mythos Preview does not support forced tool use. Use `tool_choice: {"type": "auto"}` (the default) or `tool_choice: {"type": "none"}` and rely on prompting to influence tool selection.
</Note>
