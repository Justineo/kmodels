# Model IDs and versioning

Each Claude model ID identifies a pinned version of the model.

Starting with the Claude 4.6 generation, model IDs use a dateless format. The dateless ID is the canonical model ID for that release.

Before the 4.6 generation, an alias is a convenience pointer that resolves to the most recent dated snapshot.
