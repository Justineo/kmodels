# Versions

Version 2 is now the default for API requests.

Within a version, New optional request parameters may be added and New fields may be added to
responses. Breaking changes only happen in new API versions.

Override the default with the `X-Cerebras-Version-Patch` header.
