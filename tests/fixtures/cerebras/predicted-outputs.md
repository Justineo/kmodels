# Predicted Outputs

The response reports `rejected_prediction_tokens`. Rejected tokens are billed at the output token
rate. Customers with dedicated endpoints are not affected by rejected-token pricing.
