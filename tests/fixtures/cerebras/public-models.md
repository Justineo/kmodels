# Public models

Retrieve public models with Pricing per token in USD.

- `prompt`: Cost per prompt token.
- `completion`: Cost per completion token.

The compatibility format also exposes `input_cache_read`, typically `"0"`; it is a platform
placeholder rather than the native cache price.
