# Create a batch

POST https://api.cohere.com/v2/batches

The response reports `input_tokens`, `output_tokens`, `num_successful_records`, and
`num_failed_records`. The request accepts a model name but this page does not enumerate an
exhaustive supported-model list or publish a price.
