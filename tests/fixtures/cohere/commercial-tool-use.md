# Tool use overview

Tools are functions and services defined by the caller. The model returns a tool call, the caller
must execute the tool, and the caller sends the result back in a later Chat request.
