# How Does Cohere's Pricing Work?

The response contains `billed_units` with `input_tokens` and `output_tokens`, as well as generic
`tokens`. The billed tokens are the tokens that you're actually billed for.

Trial API key usage is free but limited.
