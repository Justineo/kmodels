# Private Deployment Overview

In a private deployment, you manage the model deployment infrastructure with Cohere guidance and
support. On-premises (on-prem) deployments use customer hardware. A cloud deployment uses a virtual
private cloud (VPC) and infrastructure procured from the selected cloud provider.
