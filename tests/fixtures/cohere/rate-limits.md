# Different Types of API Keys and Rate Limits

Cohere offers evaluation keys (free but limited in usage) and production keys (paid and much less
limited in usage).

Prod keys work like trial keys for newer model variants.

Trial keys (and prod keys on newer Chat model variants) are limited to 1,000 API calls a month.
