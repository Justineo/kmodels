Billing usage: cost and consumption for your Organization over a billing period.
Retrieve cost and consumption for the Organization. The `month`, `year`, and `workspace_id` parameters are optional.
GET /v1/admin/usage?month=5&year=2026
The response includes chat, completion, ocr, audio, connectors, libraries_api, fine_tuning, and vibe_usage, together with currency.
