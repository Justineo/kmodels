# Agent tools

Built-in tools are executed server-side. `web_search`, `web_search_premium`, and
`code_interpreter` work with the Conversations API and Agents API. They are not
supported by Chat Completions. `image_generation` is also supported by Chat Completions.
