# Image generation

The `image_generation` built-in tool returns a `tool.execution` entry. A completed image is a
`tool_file` chunk with a `file_id`, `file_name`, and PNG `file_type`. Final usage contains
`connectors.image_generation`.
