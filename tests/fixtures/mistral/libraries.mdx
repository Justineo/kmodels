# Libraries

Documents have `processing_status` values `Running` and `Completed`. The
`document_library` built-in tool returns a `tool.execution` entry. Final usage includes
`connector_tokens` and `connectors.document_library`.
