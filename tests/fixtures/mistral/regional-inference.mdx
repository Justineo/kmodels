Regional endpoints only serve models hosted in that region.
Regional inference is billed at **1.1× standard list pricing** for input tokens, output tokens, cached reads, and cache writes.
Stateful features, including Agents, Batch, and the Files API, are not available on regional endpoints.
