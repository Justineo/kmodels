export const AVAILABLE_FEATURES = {
  "chat-completions": { endpoints: ["chat-completions"] },
  "function-calling": { endpoints: ["chat-completions", "conversations"] },
  "agents-conversations": { endpoints: ["agents", "conversations"] },
  connectors: { endpoints: ["agents", "conversations"] },
  "structured-outputs": { endpoints: ["chat-completions", "conversations"] },
  "predicted-outputs": { endpoints: ["chat-completions", "conversations"] },
  prefix: { endpoints: ["chat-completions", "conversations"] },
  ocr: { endpoints: ["ocr"] },
  "annotations-structured-ocr": { endpoints: ["ocr"] },
  "bbox-extraction": { endpoints: ["ocr"] },
  "document-qna": { endpoints: ["chat-completions", "conversations"] },
  fim: { endpoints: ["fim-completions"] },
  embeddings: { endpoints: ["embeddings"] },
  moderations: { endpoints: ["moderations"] },
  "chat-moderations": { endpoints: ["chat-moderations"] },
  transcriptions: { endpoints: ["audio-transcriptions"] },
  tts: { endpoints: ["audio-speech"] },
  timestamps: { endpoints: ["audio-transcriptions"] },
  batching: { endpoints: ["batch"] },
} as const;
