# Anthropic compatibility

The response supports `usage` (`input_tokens`, `output_tokens`).

Token counts are approximations based on the underlying model's tokenizer.

Unsupported features include Prompt caching and `cache_control` blocks for caching prefixes.
