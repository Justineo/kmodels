# Introduction

## Base URL

The local API is served at `http://localhost:11434/api`. For cloud models, the same API is
available at `https://ollama.com/api`.

## Versioning

Ollama's API isn't strictly versioned, but the API is expected to be stable and backwards
compatible. Deprecations are rare and will be announced in the release notes.
