paths:
  /api/generate:
    post: {}
  /api/chat:
    post: {}
  /api/embed:
    post: {}
components:
  schemas:
    Usage:
      properties:
        prompt_eval_count:
          type: integer
        eval_count:
          type: integer
