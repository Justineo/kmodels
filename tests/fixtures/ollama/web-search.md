# Web search

A free Ollama account is required.

Use `POST https://ollama.com/api/web_search` with an API key.

Use `POST https://ollama.com/api/web_fetch` to fetch a web page with the same API key.
