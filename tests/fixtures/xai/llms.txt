===/developers/model-capabilities/text/multi-agent===
# Multi Agent

> This feature is currently in **beta**.

```sh
curl https://api.x.ai/v1/responses -d '{"model": "grok-4.20-multi-agent"}'
```

### Supported Models

* `grok-4.20-multi-agent`


===/developers/model-capabilities/text/generate-text===
# Generate Text

```sh
curl https://api.x.ai/v1/responses -d '{"model": "grok-4.5"}'
```


===/developers/model-capabilities/legacy/chat-completions===
# Chat Completions

```sh
curl https://api.x.ai/v1/chat/completions -d '{"model": "grok-4.5"}'
```


===/developers/model-capabilities/images/generation===
# Image Generation

```sh
curl https://api.x.ai/v1/images/generations -d '{"model": "grok-imagine-image-quality"}'
```


===/developers/model-capabilities/images/editing===
# Image Editing

```sh
curl https://api.x.ai/v1/images/edits -d '{"model": "grok-imagine-image-quality"}'
```


===/developers/model-capabilities/video/generation===
# Video Generation

```sh
curl https://api.x.ai/v1/videos/generations -d '{"model": "grok-imagine-video"}'
```


===/developers/model-capabilities/imagine===
# Imagine

```sh
curl https://api.x.ai/v1/videos/generations -d '{"model": "grok-imagine-video-1.5"}'
```


===/developers/advanced-api-usage/batch-api===
# Batch API

`grok-4.5` is not currently supported for Batch API requests.

| Endpoint |
|---|
| `/v1/chat/completions` |
| `/v1/responses` |
| `/v1/images/generations` |
| `/v1/images/edits` |
| `/v1/videos/generations` |
| `/v1/videos/edits` |
| `/v1/videos/extensions` |

Each result includes `cost_in_usd_ticks`, and the batch summary exposes
`total_cost_usd_ticks`.

For chat results, `result.response.usage` contains `prompt_tokens`,
`prompt_tokens_details.text_tokens`, `prompt_tokens_details.image_tokens`,
`prompt_tokens_details.cached_tokens`, `completion_tokens`, and
`completion_tokens_details.reasoning_tokens`.

For Responses results, `result.response.usage` contains `input_tokens`,
`input_tokens_details.cached_tokens`, and `output_tokens`.

For image requests, use `result.image_response`, which provides `data`, `usage`, and `model`.
For video requests, use `result.video_response`, which provides `duration`, `usage`, and `model`.


===/developers/model-capabilities/text/streaming===
# Streaming

Streaming is supported by all models with text output capability.
Streaming is not supported by models with image output capability.


===/developers/model-capabilities/audio/speech-to-speech===
# Speech to Speech API

wss://api.x.ai/v1/realtime?model=grok-voice-latest

| Model | Description |
|---|---|
| `grok-voice-latest` | Alias for `grok-voice-think-fast-1.0`. Updates to `grok-voice-think-fast-2.0` on August 5, 2026. |
| `grok-voice-think-fast-2.0` | Flagship voice model |
| `grok-voice-think-fast-1.0` | Previous-generation voice model |

The Voice API supports `file_search`, `web_search`, `x_search`, and `mcp` server-side tools.

Audio pricing is based on audio duration: audio sent or received is billable.
Every `conversation.item.create` event is a text input except `function_call_output` items and
items whose content is `input_audio` or `audio`.


===/developers/model-capabilities/audio/custom-voices===
# Custom Voices

Get started in the console and create up to 30 custom voices for free. `POST /v1/custom-voices` is gated to
teams on an Enterprise plan. Custom voices work with `POST /v1/tts` and
`wss://api.x.ai/v1/realtime`.


===/developers/advanced-api-usage/context-compaction===
# Context Compaction

Call `POST /v1/responses/compact` with `"model": "grok-4.5"`. The response reports
`usage.input_tokens` and `usage.output_tokens`.


===/developers/tools/image-generation===
# Image Generation Tool

The Responses `image_generation` tool uses `grok-imagine-image-quality` and returns an
`image_generation_call` whose `result` contains the completed image. The tool takes no size or
format parameters.


===/developers/faq/security===
# Security

API requests and responses are retained for 30 days by default. Zero Data Retention disables
Stateful Responses, Files, Collections, Batch, deferred completions, and stored media output.


===/developers/models===
# Models

### Text API Pricing

| Model | Context | Input / 1M tokens | Cached input / 1M tokens | Output / 1M tokens |
|---|---|---|---|---|
| grok-4.5 (< 200k prompt tokens) | 500k | $2.00 | $0.30 | $6.00 |
| grok-4.5 (≥ 200k prompt tokens) | 500k | $4.00 | $0.60 | $12.00 |
| grok-4.3 (< 200k prompt tokens) | 1M | $1.25 | $0.20 | $2.50 |
| grok-4.3 (≥ 200k prompt tokens) | 1M | $2.50 | $0.40 | $5.00 |
| grok-build-0.1 (< 200k prompt tokens) | 256k | $1.00 | $0.20 | $2.00 |
| grok-build-0.1 (≥ 200k prompt tokens) | 256k | $2.00 | $0.40 | $4.00 |
| grok-4.20-multi-agent-0309 (< 200k prompt tokens) | 1M | $1.25 | $0.20 | $2.50 |
| grok-4.20-multi-agent-0309 (≥ 200k prompt tokens) | 1M | $2.50 | $0.40 | $5.00 |

### Imagine Pricing

| Model | Cost |
|---|---|
| grok-imagine-image-quality | $0.05 / image |
| grok-imagine-video | $0.050 / sec |
| grok-imagine-video-1.5 | $0.080 / sec |

### Voice Pricing

| Mode | Cost |
|---|---|
| Speech to Speech (grok-voice-think-fast-1.0) | $0.05 / min ($3.00 / hr) audio<br />$0.004 / text input |
| Speech to Speech (grok-voice-think-fast-2.0) | $0.08 / min ($4.80 / hr) audio<br />$0.004 / text input |
| Text to Speech | $15.00 / 1M chars |
| Speech to Text | $0.10 / hr (REST), $0.20 / hr (Streaming) |

## Model Aliases

* `<modelname>` is aliased to the latest stable version.
* `<modelname>-latest` is aliased to the latest version.
* `<modelname>-<date>` refers directly to a specific model release.


===/developers/pricing===
# Pricing

### Text API Pricing

| Model | Context | Input / 1M tokens | Cached input / 1M tokens | Output / 1M tokens |
|---|---|---|---|---|
| grok-4.5 (< 200k prompt tokens) | 500k | $2.00 | $0.30 | $6.00 |
| grok-4.5 (≥ 200k prompt tokens) | 500k | $4.00 | $0.60 | $12.00 |
| grok-4.3 (< 200k prompt tokens) | 1M | $1.25 | $0.20 | $2.50 |
| grok-4.3 (≥ 200k prompt tokens) | 1M | $2.50 | $0.40 | $5.00 |
| grok-build-0.1 (< 200k prompt tokens) | 256k | $1.00 | $0.20 | $2.00 |
| grok-build-0.1 (≥ 200k prompt tokens) | 256k | $2.00 | $0.40 | $4.00 |
| grok-4.20-multi-agent-0309 (< 200k prompt tokens) | 1M | $1.25 | $0.20 | $2.50 |
| grok-4.20-multi-agent-0309 (≥ 200k prompt tokens) | 1M | $2.50 | $0.40 | $5.00 |

### Imagine Pricing

| Model | Cost |
|---|---|
| grok-imagine-image-quality | $0.05 / image |
| grok-imagine-video | $0.050 / sec |
| grok-imagine-video-1.5 | $0.080 / sec |

### Voice Pricing

| Mode | Cost |
|---|---|
| Speech to Speech (grok-voice-think-fast-1.0) | $0.05 / min ($3.00 / hr) audio<br />$0.004 / text input |
| Speech to Speech (grok-voice-think-fast-2.0) | $0.08 / min ($4.80 / hr) audio<br />$0.004 / text input |
| Text to Speech | $15.00 / 1M chars |
| Speech to Text | $0.10 / hr (REST), $0.20 / hr (Streaming) |

## Tools Pricing

| Tool | Tool Name | Description | Cost / 1k Calls |
|---|---|---|---|
| Web Search | `web_search` | Search the web | $5 |
| X Search | `x_search` | Search X | $5 |
| Code Execution | `code_execution`, `code_interpreter` | Run code | $5 |
| File Attachments | `attachment_search` | Search attachments | $10 |
| Collections Search | `collections_search`, `file_search` | Search collections | $2.50 |

Image Search is part of Web Search and is billed at the standard Web Search rate.
For Remote MCP tools, you will not be charged for the tool invocation.
For the view image and view x video tools, you will not be charged for the tool invocation.

## Batch API Pricing

**20% off standard rates**
- grok-4.3
- grok-4.20-multi-agent-0309

Models not listed above have no batch discount.
The batch discount applies to all token types.
Image and video generation are supported in the Batch API but are billed at standard rates.

## Priority Processing Pricing

Priority processing is billed at a **2x** premium.

| | Standard | Priority |
|---|---|---|
| Token pricing | Standard rates | **2x** standard rates |

The 2x multiplier applies to all token types.

## Files and Collections Pricing

| Resource | Rate |
|---|---|
| File storage | $0.025 / GiB / day |
| Collection storage | $0.10 / GiB / day |
| File downloads | $0.20 / GiB downloaded |
| Collection downloads | $0.20 / GiB downloaded |

## Usage Guidelines Violation Fee

For violations that are caught before generation in the Responses API, we will charge a $0.05
usage guideline violation fee per request.

## Billing and Availability

Your model access might vary depending on various factors such as geographical location, account limitations, etc.


===/console/billing===
# Manage Billing

There are two billing options: **Prepaid credits** and **Monthly invoiced billing**.
Monthly invoiced billing is disabled by default.
Bank transfers take 2-3 business days. Auto top-up has a minimum $25 purchase, and warnings
include when only 1 of the 5 top-ups per 24 hours remains.
With the default $0 invoiced billing limit, API requests are automatically rejected once your prepaid credits are depleted.
xAI will use your prepaid credits first before monthly invoicing.


===/console/usage===
# Usage Explorer

By default, the usage is calculated by cost in USD. Select Dimension -> Tokens or
Dimension -> Billing items. Comparisons can use Group by: API Key. Filters include a particular
API key, a model, a request IP, a cluster, or the token type.


===/developers/cost-tracking===
# Cost Tracking

Every inference response includes the exact cost you were charged in `cost_in_usd_ticks`. The value
is per-request, after all applicable discounts, and inclusive of all token costs and server-side tool invocation costs.

1 USD = 10,000,000,000 ticks.

For REST streaming, Cost is only included in the final chunk.
Set `stream_options: {"include_usage": true}` to receive the terminal usage object.


===/developers/advanced-api-usage/prompt-caching/usage-and-pricing===
# Usage & Pricing

Chat reports `usage.prompt_tokens_details.cached_tokens` and Responses reports
`usage.input_tokens_details.cached_tokens`.

| Token type | Billing rate |
|---|---|
| Reasoning tokens | Full completion token price |

Long context pricing applies to total prompt tokens (including cached tokens).


===/developers/advanced-api-usage/priority-processing===
# Priority Processing

Request `service_tier: "priority"`. You are only billed at the priority rate when the response confirms `"priority"`.

```json
{"usage":{"cost_in_usd_ticks":37756000}}
```


===/developers/tools/tool-usage-details===
# Tool Usage Details

### `server_side_tool_usage` - Successful Calls (Billable)

This value determines your billing. Only successful tool executions are billed.

Agent usage reports `completion_tokens`, cumulative `prompt_tokens`, uncached
`prompt_text_tokens`, `reasoning_tokens`, `cached_prompt_text_tokens`, and
`prompt_image_tokens`.

`server_side_tool_usage` reports these successful billable categories:

* `SERVER_SIDE_TOOL_WEB_SEARCH`
* `SERVER_SIDE_TOOL_X_SEARCH`
* `SERVER_SIDE_TOOL_CODE_EXECUTION`
* `SERVER_SIDE_TOOL_COLLECTIONS_SEARCH`


===/developers/rest-api-reference/inference/chat===
# Chat

Chat Completions response:

```json
{"object":"chat.completion","service_tier":"priority","usage":{"prompt_tokens":135,"prompt_tokens_details":{"text_tokens":125,"image_tokens":10,"cached_tokens":98},"completion_tokens":48,"completion_tokens_details":{"reasoning_tokens":12}}}
```

Responses response:

```json
{"object":"response","service_tier":"default","usage":{"input_tokens":125,"input_tokens_details":{"cached_tokens":98},"output_tokens":48,"output_tokens_details":{"reasoning_tokens":12}}}
```


===/developers/rest-api-reference/inference/images===
# Images

Image generation and image edit request bodies accept `resolution` and `quality`. The effective
values include provider defaults. Image edit accepts an `image` object or an `images` array.

Response body: `data` array of completed generated image objects.


===/developers/rest-api-reference/inference/videos===
# Videos

The request body accepts an `image`, a video input, and an effective output `resolution`.
Video input may be supplied by URL, base64 data, or file ID.

```json
{"status":"done","video":{"url":"https://example.test/video.mp4","duration":6},"model":"grok-imagine-video"}
```


===/developers/rest-api-reference/management===
# Management REST API Overview

You need a management key. The base URL is https://management-api.x.ai.


===/developers/management-api-guide===
# Using Management API

You need a management key, which is separate from your API key. The base is
https://management-api.x.ai. ACLs include `api-key:model:*` and `api-key:endpoint:*`.
API keys support `"qps"`, `"qpm"`, and `"tpm"` limits.
There can be a slight delay between creating an API key and propagation across clusters.

GET https://management-api.x.ai/auth/teams/{teamId}/models

GET https://management-api.x.ai/auth/teams/{teamId}/endpoints


===/developers/rest-api-reference/management/billing===
# Billing Management

## POST /v1/billing/teams/\{team\_id}/usage

Get historical usage of the API over a time period, aggregated by fields.

`TIME\_UNIT\_SECOND` is supported. The response includes `limitReached`.

```json
{"analyticsRequest":{"values":[{"name": "usd","aggregation":"AGGREGATION_SUM"}]}}
```


===/developers/rest-api-reference/inference/models===
# Models

## GET /v1/models

### Response Body

* `data` (array<object>, required)

  * `aliases` (array<string>, required)
  * `cached_prompt_text_token_price` (integer | null)
  * `cached_prompt_text_token_price_long_context` (integer | null)
  * `completion_text_token_price` (integer | null)
  * `completion_text_token_price_long_context` (integer | null)
  * `context_length` (integer | null)
  * `created` (integer, required)
  * `id` (string, required)
  * `image_price` (integer | null)
  * `long_context_threshold` (integer | null)
  * `object` (string, required)
  * `owned_by` (string, required)
  * `prompt_image_token_price` (integer | null)
  * `prompt_text_token_price` (integer | null)
  * `prompt_text_token_price_long_context` (integer | null)

* `object` (string, required)

**Response example:**

```json
{"data":[{"id":"grok-4.5","aliases":[],"context_length":500000,"created":1776556800,"object":"model","owned_by":"xai","prompt_text_token_price":20000}],"object":"list"}
```

## GET /v1/models/\{model\_id}

### Response Body

* `aliases` (array<string>, required)
* `cached_prompt_text_token_price` (integer | null)
* `cached_prompt_text_token_price_long_context` (integer | null)
* `completion_text_token_price` (integer | null)
* `completion_text_token_price_long_context` (integer | null)
* `context_length` (integer | null)
* `created` (integer, required)
* `id` (string, required)
* `image_price` (integer | null)
* `long_context_threshold` (integer | null)
* `object` (string, required)
* `owned_by` (string, required)
* `prompt_image_token_price` (integer | null)
* `prompt_text_token_price` (integer | null)
* `prompt_text_token_price_long_context` (integer | null)

**Response example:**

```json
{"id":"grok-4.5","created":1776556800,"object":"model","owned_by":"xai","prompt_text_token_price":20000}
```

## GET /v1/language-models

### Response Body

* `models` (array<object>, required)

  * `aliases` (array<string>, required)
  * `cached_prompt_text_token_price` (integer, required)
  * `cached_prompt_text_token_price_long_context` (integer, required)
  * `completion_text_token_price` (integer, required)
  * `completion_text_token_price_long_context` (integer, required)
  * `created` (integer, required)
  * `fingerprint` (string, required)
  * `id` (string, required)
  * `input_modalities` (array<string>, required)
  * `long_context_threshold` (integer, required)
  * `object` (string, required)
  * `output_modalities` (array<string>, required)
  * `owned_by` (string, required)
  * `prompt_image_token_price` (integer, required)
  * `prompt_text_token_price` (integer, required)
  * `prompt_text_token_price_long_context` (integer, required)
  * `search_price` (integer, required)
  * `version` (string, required)

**Response example:**

```json
{"models":[{"id":"grok-4.5","fingerprint":"fp","created":1776556800,"object":"model","owned_by":"xai","version":"1.0","input_modalities":["text"],"output_modalities":["text"],"aliases":[]}]}
```

## GET /v1/language-models/\{model\_id}

### Response Body

* `aliases` (array<string>, required)
* `cached_prompt_text_token_price` (integer, required)
* `cached_prompt_text_token_price_long_context` (integer, required)
* `completion_text_token_price` (integer, required)
* `completion_text_token_price_long_context` (integer, required)
* `created` (integer, required)
* `fingerprint` (string, required)
* `id` (string, required)
* `input_modalities` (array<string>, required)
* `long_context_threshold` (integer, required)
* `object` (string, required)
* `output_modalities` (array<string>, required)
* `owned_by` (string, required)
* `prompt_image_token_price` (integer, required)
* `prompt_text_token_price` (integer, required)
* `prompt_text_token_price_long_context` (integer, required)
* `search_price` (integer, required)
* `version` (string, required)

**Response example:**

```json
{"id":"grok-4.5","fingerprint":"fp","created":1776556800,"object":"model","owned_by":"xai","version":"1.0","input_modalities":["text"],"output_modalities":["text"],"aliases":[]}
```

## GET /v1/image-generation-models

### Response Body

* `models` (array<object>, required)

  * `aliases` (array<string>, required)
  * `created` (integer, required)
  * `fingerprint` (string, required)
  * `id` (string, required)
  * `image_price` (integer, required)
  * `input_modalities` (array<string>, required)
  * `max_prompt_length` (integer, required)
  * `object` (string, required)
  * `output_modalities` (array<string>, required)
  * `owned_by` (string, required)
  * `version` (string, required)

**Response example:**

```json
{"models":[{"id":"grok-imagine-image","fingerprint":"fp","max_prompt_length":1024,"created":1738961600,"object":"model","owned_by":"xai","version":"1.0","prompt_text_token_price":100000,"prompt_image_token_price":100000,"generated_image_token_price":100000,"aliases":[]}]}
```

## GET /v1/image-generation-models/\{model\_id}

### Response Body

* `aliases` (array<string>, required)
* `created` (integer, required)
* `fingerprint` (string, required)
* `id` (string, required)
* `image_price` (integer, required)
* `input_modalities` (array<string>, required)
* `max_prompt_length` (integer, required)
* `object` (string, required)
* `output_modalities` (array<string>, required)
* `owned_by` (string, required)
* `version` (string, required)

**Response example:**

```json
{"id":"grok-imagine-image","fingerprint":"fp","max_prompt_length":1024,"created":1738961600,"object":"model","owned_by":"xai","version":"1.0","prompt_text_token_price":100000,"prompt_image_token_price":100000,"generated_image_token_price":100000,"aliases":[]}
```

## GET /v1/video-generation-models

### Response Body

* `models` (array<object>, required)

  * `aliases` (array<string>, required)
  * `created` (integer, required)
  * `fingerprint` (string, required)
  * `id` (string, required)
  * `input_modalities` (array<string>, required)
  * `object` (string, required)
  * `output_modalities` (array<string>, required)
  * `owned_by` (string, required)
  * `version` (string, required)

**Response example:**

```json
{"models":[{"id":"grok-imagine-video","fingerprint":"fp","created":1743724800,"object":"model","owned_by":"xai","version":"1.0","input_modalities":["text"],"output_modalities":["video"],"aliases":[]}]}
```

## GET /v1/video-generation-models/\{model\_id}

### Response Body

* `aliases` (array<string>, required)
* `created` (integer, required)
* `fingerprint` (string, required)
* `id` (string, required)
* `input_modalities` (array<string>, required)
* `object` (string, required)
* `output_modalities` (array<string>, required)
* `owned_by` (string, required)
* `version` (string, required)

**Response example:**

```json
{"id":"grok-imagine-video","fingerprint":"fp","created":1743724800,"object":"model","owned_by":"xai","version":"1.0","input_modalities":["text"],"output_modalities":["video"],"aliases":[]}
```


===/developers/faq/billing===
# Billing

When you make API requests, the cost is calculated immediately.

The inference endpoints add pre-defined tokens to help process the request.


===/developers/rate-limits===
# Rate Limits

Your tier is based on cumulative spend. Rate limit tiers apply to text and embedding models.


===/developers/rest-api-reference/inference/voice===
# Voice

POST /v1/tts

Text to Speech request body: `text` string containing the characters to convert to speech.

Streaming Text to Speech accepts `text.delta` events followed by `text.done`; billing remains per
input character.

POST /v1/stt

Speech to Text - REST returns `duration`, the audio duration in seconds.

Streaming Speech to Text ends with `transcript.done`, where duration is always present.

WSS wss://api.x.ai/v1/realtime


===/developers/migration/may-15-retirement===
# Grok Model Retirement on May 15, 2026

Effective May 15, 2026 at 12:00 PM PT, the following models will be retired:

* `grok-3`
* `grok-imagine-image-pro`

### How the redirects work

Requests using these retired model slugs will continue to resolve and automatically redirect to
the replacement model.

## Recommended Replacements

| Model being retired | Redirect target after May 15 |
|---|---|
| `grok-3` | `grok-4.3` with `none` reasoning effort |
| `grok-imagine-image-pro` | `grok-imagine-image-quality` |


===/developers/release-notes===
# Release Notes

## July

### Grok 4.5

Grok 4.5 is now available.

## May

### Grok Build is available

Use `grok-build-0.1` for coding.

## April

### Grok Voice Think Fast 1.0 is available

You can now use `grok-voice-think-fast-1.0`.

### Grok Voice Think Fast 2.0 is available

You can now use `grok-voice-think-fast-2.0`.

## March

### Grok 4.20 Multi-agent is live

Use `grok-4.20-multi-agent` for multi-agent research.

## December 2025

### Grok Voice Agent API is released

The Voice Agent API is generally available.
